---
apiVersion: v1
kind: Service
metadata:
  name: clusterdos-vllm
  labels:
    app: vllm
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vllm
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
